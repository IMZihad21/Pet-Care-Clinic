import React from 'react';
import useData from '../../../Hooks/useData';

const MiniServices = () => {
    const data = useData();
    console.log(data)
    return (
        <div>
            
        </div>
    );
};

export default MiniServices;